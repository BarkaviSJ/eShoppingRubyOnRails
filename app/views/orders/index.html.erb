<p id="notice"><%= notice %></p>

<h1>Order status available for the orders placed from your account</h1>

<table>
  <thead>
    <tr>
      <th>Status</th>
      <th>Customer</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.status %></td>
        <td><%= order.customer_id %></td>
        <td><%= link_to 'Show', order %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
